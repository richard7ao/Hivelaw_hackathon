# Copy to .env and fill in. The script reads ANTHROPIC_API_KEY from the environment.
ANTHROPIC_API_KEY=sk-ant-...

# Optional knobs (defaults shown):
# STEELMAN_MODEL=claude-opus-4-8
# STEELMAN_EFFORT=high          # low | medium | high | xhigh | max
# STEELMAN_CONCURRENCY=3        # how many cases to run at once
